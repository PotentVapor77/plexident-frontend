services:
  frontend:
    build: .
    container_name: react-app-dev
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/node_modules 
    environment:
      - WATCHPACK_POLLING=true 
    stdin_open: true 
    tty: true